interface:
  display_name: "Ultrareview"
  short_description: "Portable adversarial review using staged artifacts"
  default_prompt: "Use $ultrareview to review this bounded code change or topic with strict qualification gates and an intuitive report in my requested language."

policy:
  allow_implicit_invocation: false
